// Local development placeholder.
// In production this file is regenerated at container start by docker-entrypoint.sh
// from the API_BASE_URL / APP_ENV environment variables of the Cloud Run service.
window.__APP_CONFIG__ = {
  // Empty string => call the same origin ("/api/..."), which the Vite dev server
  // proxies to the backend (see VITE_DEV_API_PROXY in vite.config.ts).
  API_BASE_URL: '',
  APP_ENV: 'development',
};
